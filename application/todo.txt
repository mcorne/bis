* enable foreign key support for each connection: PRAGMA foreign_keys = ON;
* sha1() user password on the client side so they do not pass on the clear over http
* set default time zone for displaying purposes as config param, check time zone in SQLite
* set adminer to enforce foreign key constraints on update or delete
* set adminer to hash password
